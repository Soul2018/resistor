/** this is the application stateless component header */

import React from 'react';
import './Header.css';


const Header = () => {

    return (
        <header className="header">
        <h1 className="App-title">Calculates the Ohm value of a resistor based on the band colors.</h1>
        </header>
    );
};

export default Header;


